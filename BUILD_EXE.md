# بناء ملف .exe فعلي

## ليه مش مبني ليك جاهز في الحزمة؟

الحقيقة التقنية البسيطة: ملفات `.exe` الحقيقية لازم تتبني **على نفس
نظام التشغيل المستهدف نفسه** (يعني ويندوز يبني ليه .exe يشتغل على
ويندوز). أنا شغال في بيئة Linux، فمقدرش أنتجلك .exe ويندوز فعلي يشتغل
صح من هنا - أي حد يقولك عكس ده بيوعدك بحاجة مش هتشتغل بشكل موثوق.

الحل الصحيح: سكريبت واحد تشغّله انت على جهاز الويندوز بتاعك، وهو اللي
هيولّد الـ `.exe` فعليًا على نفس الجهاز اللي هيشتغل عليه بعدين.

## الخطوات (على جهاز الويندوز، بعد ما تخلص تثبيت المشروع عادي)

```bash
# 1. تأكد إن البيئة الافتراضية مفعّلة
venv\Scripts\activate.bat

# 2. ثبّت أداة البناء
pip install pyinstaller

# 3. ابني الـ exe
pyinstaller --onefile --name Jarvis --console cli.py
```

بعد ما يخلص (بياخد دقيقة أو اتنين)، هتلاقي الملف هنا:

```
dist\Jarvis.exe
```

ده ملف تنفيذي حقيقي، تقدر تنقله لأي مكان وتشغّله بالضغط عليه مباشرة.

## نقاط مهمة لازم تعرفها عن الـ .exe الناتج

1. **لسه محتاج مفتاح API**: الـ exe مش بيحمل مفتاح Claude API جواه
   (وده مقصود لأسباب أمان - متخليش المفتاح داخل ملف تقدر توزعه).
   لازم يكون متحدد كمتغير بيئة `ANTHROPIC_API_KEY` على أي جهاز
   هيشتغل عليه الـ exe.

2. **حجمه هيكبر**: الـ exe بيحمل جوه نسخة كاملة من بايثون ومكتباته،
   فمتوقع يكون حجمه 30-50 ميجا بدل الكيلوبايتات القليلة بتاعة الكود
   المصدري - ده طبيعي وليس خطأ.

3. **الأدوات الاختيارية (Gmail، أوفيس، صور) لازم تتثبت قبل البناء**:
   لو عايز الـ exe يدعم ميزة معينة، ثبّتها بـ `pip install -r
   requirements-...txt` **قبل** ما تشغّل أمر `pyinstaller`، عشان
   PyInstaller بيحزم بس المكتبات اللي لاقيها متثبتة وقت البناء.

4. **محتاج إنترنت وقت التشغيل برضه**: الـ exe نفسه ملف محلي، لكن
   الوكيل لسه بيتواصل مع Claude API عبر الإنترنت في كل رسالة - مفيش
   "أوفلاين" هنا.

## بديل أسرع لو مش محتاج .exe فعلي دلوقتي

لو الهدف بس إنك "تدوس دبل كليك بدل ما تكتب أوامر"، فيه حل أسهل بكتير
من غير ما تحتاج PyInstaller خالص: ملف `.bat` بسيط بيشغّل كل حاجة
نيابة عنك. لو حابب، قولّي وهعملهولك كملف منفصل (`Jarvis.bat`) تحطه
على سطح المكتب وتدوس عليه دبل كليك يفتحلك الوكيل مباشرة.
